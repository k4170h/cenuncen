import ReactDOM from 'react-dom';
import React from 'react';

const Viewer = () => {
  return <></>;
};

ReactDOM.render(
  <React.StrictMode>
    <Viewer />
  </React.StrictMode>,
  document.getElementById('root')
);
